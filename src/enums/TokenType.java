package enums;

public enum TokenType {
    // Literal Types
    NUMBER,
    IDENTIFIER,

    // Keywords
    LET,

    // Grouping & Operators
    BINARY_OPERATOR,
    EQUALS,
    OPEN_PAREN,
    CLOSE_PAREN
}

